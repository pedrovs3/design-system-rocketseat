{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "turbo": "turbo run dev --parallel",
    "turbo:build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.10.15"
  }
}
